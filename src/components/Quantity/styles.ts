import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { flex: 1, flexDirection: "row", alignItems: "center" },
  label: { fontSize: 18 },
  quantity: { fontSize: 18, height: 24, margin: 5 },
});

export default styles;
