import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#FFF",
    marginTop: 125,
    width: "95%",
    borderRadius: 10,
  },
});

export default styles;
